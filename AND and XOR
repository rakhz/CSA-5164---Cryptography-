#include <stdio.h>

int main() {
    char *str = "Hello world";
    while (*str) {
        printf("Character: %c, AND with 127: %c, XOR with 127: %c\n", *str, *str & 127, *str ^ 127);
        str++;
    }
    return 0;
}
